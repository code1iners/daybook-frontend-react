// Authenticate
export enum AuthRoutes {
  Login = "/login",
}

// Diary.
export enum DiaryRoutes {
  Diaries = "/diaries",
}

// Others
export enum OtherRoutes {
  Root = "/",
  Settings = "/settings",
  All = "*",
}

export enum UserRoutes {
  Me = "/me",
}
