// Root
export const rootPath = "/";

// Authenticate
export const authRoutes = {
  login: "/login",
};

// Diary.
export const diaryRoutes = {
  diaries: "/diaries",
};

// Others
export const otherRoutes = {
  all: "*",
};
